name: Build MLS Data
on:
  push:
    paths:
      - "docs/data/mls/matches/**"
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Clear old data files
        run: |
          echo "[]" > docs/data/mls/players.json
          echo "[]" > docs/data/mls/teams.json
          echo "[]" > docs/data/mls/team-stats.json
          echo "{}" > docs/data/mls/match-records.json
      - name: Run build script
        run: |
          cd $GITHUB_WORKSPACE
          python scripts/build_mls_data.py
      - name: Commit and push generated files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/mls/players.json \
                  docs/data/mls/teams.json \
                  docs/data/mls/team-stats.json \
                  docs/data/mls/match-records.json
          git diff --cached --quiet || git commit -m "chore: rebuild MLS data [skip ci]"
          git pull --rebase origin main
          git push
