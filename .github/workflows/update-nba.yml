name: Update NBA Data
on: [workflow_dispatch]

jobs:
update:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v4
- uses: actions/setup-python@v5
with:
python-version: "3.11"
- run: pip install pandas kaggle
- run: python scripts/build_nba_games.py
