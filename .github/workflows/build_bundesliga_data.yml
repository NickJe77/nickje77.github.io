name: Build Bundesliga Data
on:
  push:
    paths:
      - "docs/data/bundesliga/matches/**"
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Run build script
        run: python scripts/build_bundesliga_data.py
      - name: Commit and push generated files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/bundesliga/players.json \
                  docs/data/bundesliga/teams.json \
                  docs/data/bundesliga/team-stats.json
          git diff --cached --quiet || git commit -m "chore: rebuild Bundesliga data [skip ci]"
          git push
