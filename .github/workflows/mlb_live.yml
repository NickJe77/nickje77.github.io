name: MLB Live Updater

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"   # every 30 minutes

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      # -------------------------
      # CHECKOUT
      # -------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------
      # SETUP PYTHON
      # -------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # -------------------------
      # INSTALL DEPENDENCIES
      # -------------------------
      - name: Install dependencies
        run: |
          pip install requests

      # -------------------------
      # 🔴 CLEAN OLD FILES
      # -------------------------
      - name: Clean 2026 boxscores
        run: |
          rm -rf docs/data/baseball/boxscores/2026/*
          mkdir -p docs/data/baseball/boxscores/2026

      # -------------------------
      # RUN SCRIPT
      # -------------------------
      - name: Run MLB Builder
        run: |
          python scripts/mlb_live_boxscores.py

      # -------------------------
      # COMMIT CHANGES
      # -------------------------
      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add docs/data/baseball/boxscores/2026/

          if ! git diff --cached --quiet; then
            git commit -m "Rebuild MLB 2026 boxscores"
            git push
          else
            echo "No changes to commit"
          fi
