name: PGA Winners Builder

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"   # every Monday

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Ensure golf folders exist
        run: |
          mkdir -p docs/data/golf
          mkdir -p docs/data/golf/players

      - name: Run PGA builder
        run: |
          python scripts/pga_winners_builder.py

      - name: Commit & Push ALL golf data
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          # 🔥 THIS IS THE FIX
          git add docs/data/golf/

          git commit -m "Update PGA data (winners + players)" || echo "No changes"

          git pull --rebase origin main
          git push
