name: Golf Data Builder (PGA + LIV)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"   # every Monday

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Ensure golf folders exist
        run: |
          mkdir -p docs/data/golf
          mkdir -p docs/data/golf/players

      - name: Run Golf builders (PGA + LIV)
        run: |
          python scripts/pga_winners_builder.py
          python scripts/liv_winners_builder.py

      - name: Commit & Push ALL golf data
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add docs/data/golf/

          git commit -m "Update Golf data (PGA + LIV + players)" || echo "No changes"

          git pull --rebase origin main
          git push
