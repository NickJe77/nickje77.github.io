name: nba

on:
workflow_dispatch:

jobs:
run:
runs-on: ubuntu-latest

```
steps:
  - uses: actions/checkout@v4
  - run: pip install requests pandas
  - run: python scripts/update_recent_nba_games.py
```
