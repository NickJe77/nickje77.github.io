name: Rebuild NBA indexes

on:
  push:
    paths:
      - 'docs/data/nba/**/*.json'

jobs:
  rebuild:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: rebuild indexes
        run: |

          for dir in docs/data/nba/*; do
            if [ -d "$dir" ]; then

              season=$(basename "$dir")

              cd "$dir"

              ls *.json 2>/dev/null | grep -v index.json | sed 's/.json//' | sort > games.txt

              echo '{' > index.json
              echo "  \"season\": $season," >> index.json
              echo '  "games": [' >> index.json

              awk '{printf "    \"%s\",\n", $0}' games.txt >> index.json

              sed -i '$ s/,$//' index.json

              echo '  ]' >> index.json
              echo '}' >> index.json

              rm games.txt

              cd - > /dev/null

            fi
          done

      - name: commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add docs/data/nba/**/index.json
          git commit -m "Rebuild NBA indexes"
          git push
